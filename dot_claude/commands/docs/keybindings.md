---
description: Document commands and keybindings as rows in the keybindings table.
argument-hint: <commands, keybindings, or prose>
disable-model-invocation: true
---

Document every command and keybinding in the input below as rows in the keybindings table of the section it belongs to. One row per command: description, command, keybinding. Add rows to an existing table before starting a new one.

$ARGUMENTS
