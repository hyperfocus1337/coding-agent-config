---
allowed-tools: Bash(chezmoi add:*), Bash(chezmoi managed:*)
description: Add a file to the chezmoi source state
disable-model-invocation: true
---

Run `chezmoi add $ARGUMENTS`. If no path is given, ask for one. Report the source path chezmoi created or updated.
